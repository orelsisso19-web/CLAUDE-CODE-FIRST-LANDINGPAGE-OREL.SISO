# Server-only — used by /api/contact to forward submissions to Keshlo.
# Never prefix with NEXT_PUBLIC_, or it will be inlined into the browser bundle.
KESHLO_ENDPOINT=

# Public — used by the WhatsApp FAB. E.164 digits only, no + or spaces.
# Example: 972501234567
NEXT_PUBLIC_WHATSAPP_NUMBER=
